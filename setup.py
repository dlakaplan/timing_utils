from setuptools import setup, find_packages

setup(
    name="timing_utils",
    version="0.1.0",
    description="Utilities for NANOGrav timimg",
    author="Joe Swiggum",
    author_email="kaplan@uwm.edu",
    url="",
    packages=find_packages(exclude=("tests")),
)
